# Microsoft 3D Builder風 3Dモデル編集ツール 実装計画

Microsoft 3D Builder の核となる操作性・機能をブラウザ上で再現し、外部通信を行わずにローカル環境のみで完全動作するWebアプリケーションを構築します。

## 概要・要件整理
- **ローカル完結・通信なし**: 外部CDNや外部APIへの依存を排除し、必要なライブラリやフォント・アセットはローカル環境（Vite開発サーバーおよびビルド成果物）内で完結。
- **言語・技術スタック**:
  - フロントエンド: **HTML5 + Vanilla Modern JavaScript + CSS3**（UIフレームワークの余計なオーバーヘッドを排し、高速かつ軽量なFluent Design風UI）
  - 3Dレンダリングエンジン: **Three.js (r160+)**（ローカルnpmインストール）
  - ブーリアン演算（結合・差分・交差）: **three-bvh-csg**（Three.js公式推奨の高速・堅牢なBVHベースCSGライブラリ）
  - 3Dモデル入出力: Three.js公式の **STLLoader/STLExporter, OBJLoader/OBJExporter, GLTFLoader/GLTFExporter, PLYLoader/PLYExporter, 3MFLoader**
- **除外機能**: 印刷発注サービス機能（要件通り除外）

---

## 主な機能一覧

### 1. 3Dビューポート & キャンバス
- **グリッド・ビルドプレート**: 3D Builder特有のチェッカー/グリッド床（サイズ・間隔表示）、原点マーカー
- **カメラ・視点操作**:
  - マウス操作（オービット回転、パン、ズーム）
  - ビューキューブ / 視点切り替えボタン（上面、前面、側面、斜め/アイソメトリック）
  - 全体表示（Fit to View）
- **ライティング・描画モード**:
  - 影付きリアルタイムライティング（Directional + Ambient）
  - シェーディング切り替え（通常ソリッド、ワイヤーフレーム、X線/半透明）

### 2. オブジェクト生成（挿入メニュー）
- **基本プリミティブの生成**:
  - 立方体（Cube / Box）
  - 球体（Sphere）
  - 円柱（Cylinder）
  - 円錐（Cone）
  - 角錐（Pyramid）
  - トーラス（ドーナツ型）
  - 六角柱（Hexagonal Prism）
  - くさび（Wedge）
- **3Dテキスト生成**: 入力文字列から立体テキストメッシュを生成
- **床への自動配置**: 生成時に床面（Y=0）に接地して出現

### 3. オブジェクト操作 & トランスフォーム
- **ギズモ操作**: Three.js `TransformControls` による移動・回転・拡縮ギズモ
- **数値入力インスペクター**:
  - 位置（X, Y, Z mm）
  - 回転（X, Y, Z 度）
  - サイズ/寸法（幅 W, 奥行 D, 高さ H mm）＆ アスペクト比固定トグル
- **3D Builder特有の便利機能**:
  - **接地（Lay Flat / Settle）**: オブジェクトの底面バウンディングボックスを床面（Y=0）にピタッと落とす
  - **床に平行に吸着**: 選択した面を床に向ける（簡易整列）
  - **整列（Align）**: 複数オブジェクトのX/Y/Z軸位置合わせ（最小、中央、最大）
- **オブジェクト管理**:
  - 複数選択、複製（Ctrl+D）、削除（Delete/Backspace）、すべて選択（Ctrl+A）
  - シーングラフ/アウトライナー（パーツリスト、表示/非表示、ロック）
  - アンドゥ / リドゥ（Undo / Redo: Ctrl+Z / Ctrl+Y）

### 4. ブーリアン演算（3D Builderの看板機能）
`three-bvh-csg` を活用した本格的CSG処理：
- **結合（Union / Merge）**: 選択した複数のモデルをひとつの立体に融合（内部ポリゴンを除去）
- **型抜き / 差分（Subtract / Difference）**: 基準オブジェクトから交差するオブジェクトを削り取る
- **交差（Intersect）**: 重なり合っている体積部分だけを残す

### 5. 平面切断・スライス（Split）
3D Builderの強力なスライス機能を再現：
- 切断平面（Plane）のギズモ表示（位置および角度をドラッグや角度スライダーで微調整）
- 切断モード選択：
  - **上側を残す（Keep Top）**
  - **下側を残す（Keep Bottom）**
  - **両方保持（Split Both）**: 1つのモデルを2つの独立したオブジェクトに分割
- 切断面のポリゴンフタ処理（Cap fills）

### 6. マテリアル & カラー
- カラーピッカー（カラーホイール、パレットプリセット）
- 粗さ（Roughness）、金属感（Metalness）、不透明度（Opacity）調整

### 7. ファイルのインポート / エクスポート
- **インポート対応形式**:
  - STL（バイナリ / アスキー）
  - OBJ（+ MTL）
  - 3MF
  - GLTF / GLB
  - PLY
  - ドラッグ＆ドロップ または ファイル選択ダイアログ
- **エクスポート対応形式**:
  - STL（3Dプリンタ標準、バイナリ形式で高速・小サイズ）
  - OBJ
  - GLTF / GLB
  - PLY
  - シーン全体のエクスポート、または選択オブジェクトのみのエクスポート

### 8. UIデザイン
- Microsoft 3D BuilderおよびWindows 11 Fluent Designにインスパイアされた、モダンで透明感のある洗練されたダーク/ライト調UI
- トップツールバー（タブ式：ファイル、挿入、編集、表示、オブジェクト）
- 右サイドバー：プロパティ（位置・回転・スケール・マテリアル・寸法）
- 左サイドバー：オブジェクト一覧（アウトライナー）
- 下部ステータスバー：選択数、頂点数/ポリゴン数、寸法ガイド

---

## 開発・構成方針

### ディレクトリ構造
```
maceman_3dbuilder/
├── index.html              # メインエントリーポイント（Fluent Design UI構造）
├── package.json            # Three.js, three-bvh-csg, vite 等の依存定義
├── vite.config.js          # オフライン完全バンドル設定
├── src/
│   ├── main.js             # アプリ初期化、イベントバインド
│   ├── style.css           # 3D Builder風 Fluent Design スタイルシート
│   ├── core/
│   │   ├── Viewer.js       # Three.jsシーン、レンダラー、カメラ、ライト、床グリッド
│   │   ├── SceneManager.js # オブジェクト管理、選択状態、アウトライナー同期
│   │   ├── History.js      # Undo / Redo履歴マネージャー
│   │   └── Raycaster.js    # マウスクリックでのオブジェクト選択
│   ├── operations/
│   │   ├── BooleanOps.js   # three-bvh-csgを用いたUnion/Subtract/Intersect処理
│   │   ├── SplitOps.js     # 平面切断（スライス）処理
│   │   ├── TransformOps.js # 接地(Lay Flat)、整列(Align)、寸法変更
│   │   └── Primitives.js   # 立方体、円柱、球、文字などのメッシュ生成
│   ├── io/
│   │   ├── Importer.js     # STL, OBJ, 3MF, GLTF, PLYのローカルファイル読み込み
│   │   └── Exporter.js     # STL, OBJ, GLB, PLYのダウンロード出力
│   └── ui/
│       ├── Toolbar.js      # 上部リボンメニュー・操作切り替え
│       ├── Inspector.js    # 右側プロパティパネル
│       ├── Outliner.js     # 左側オブジェクトツリー
│       ├── ViewCube.js     # 視点切り替えギズモ/ボタン
│       └── Dialogs.js      # 各種モーダル（スライス設定、ヘルプ等）
```

---

## 検証プラン
1. **ビルドおよびローカル起動確認**:
   - `npm.cmd install` でThree.jsおよびthree-bvh-csg等の必要なパッケージをインストール
   - `npm.cmd run dev` でローカルサーバーを起動し、ブラウザ上で正常に起動・エラーがないことを確認
2. **基本操作検証**:
   - プリミティブ（直方体、球体、円柱など）の配置と床面接地
   - ギズモ操作および数値入力での移動・回転・スケール
   - Undo/Redo操作
3. **高機能検証**:
   - 2つのオブジェクトを重ねた状態での結合（Union）、型抜き（Subtract）、交差（Intersect）
   - 平面切断（Split）でモデルが半分にカットされ、切り口が閉じられること
   - 接地ボタン（Lay Flat）で回転したオブジェクトが床（Y=0）にピタッと落ちること
4. **ファイル入出力検証**:
   - テスト用STL/OBJ/GLTFファイルのインポート
   - 編集後のSTL/OBJ/GLBエクスポートと、再インポートによる形状確認
5. **完全オフライン動作確認**:
   - 外部ネットワークへのリクエストが一切発生しないことをブラウザの開発者ツールで確認
